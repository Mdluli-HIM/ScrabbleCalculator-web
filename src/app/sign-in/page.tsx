import {
  PlaceholderScreen,
} from "@/components/system/placeholder-screen";

export default function SignInPage() {
  return (
    <PlaceholderScreen
      eyebrow="Registered account"
      title="Sign In"
      description="This roue is ready. We will build its final form, validation and API authentication after receiving the Sign In Figma design."
    />
  );
}
