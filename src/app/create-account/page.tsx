import {
  PlaceholderScreen,
} from "@/components/system/placeholder-screen";

export default function CreateAccountPage() {
  return (
    <PlaceholderScreen
      eyebrow="New account"
      title="Create Account"
      description="This route is ready. We will add registration fields, validation and API integration when we reach the authentication screen sprint."
    />
  );
}
