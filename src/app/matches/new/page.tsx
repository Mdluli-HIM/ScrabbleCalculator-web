import {
  PlaceholderScreen,
} from "@/components/system/placeholder-screen";

export default function NewMatchPage() {
  return (
    <PlaceholderScreen
      eyebrow="Guest session connected"
      title="Match setup comes next."
      description="The API guest session has been created and stored safely in this browser. We will replace this placeholder after you send the Match Setup Figma design."
    />
  );
}
